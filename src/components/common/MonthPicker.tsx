// MonthPicker is now an alias for DatePickerWithRange.
// Kept for compatibility — callers should migrate to DatePickerWithRange directly.
export { DatePickerWithRange as MonthPicker } from "@/components/ui/date-range-picker"
